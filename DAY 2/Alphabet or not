#include <stdio.h>

int main() 
{
    char ab = '8';
    if
    
        ((ab >= 'A' && ab <= 'Z' ) || (ab >= 'a' && ab <= 'z'))
    {
        printf("Alphabet");
    }
    else {
    printf("not alphabet");
    }
    
}
